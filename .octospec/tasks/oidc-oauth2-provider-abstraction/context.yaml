# Rule injection record for the Implement phase.
# _global/ was not synced in this workspace, so every rule resolves to tier=repo.
injected:
  - id: space-isolation      # priority 92, load_bearing — touches: auth
    source: repo
  - id: trust-boundary       # priority 90, load_bearing — touches: trust-boundary, wire-contract
    source: repo
  - id: error-handling       # priority 85, load_bearing — touches: error-response, i18n, wire-contract
    source: repo
  - id: rate-limit           # priority 80, load_bearing — paths: modules/**/*.go
    source: repo
  - id: testing              # priority 70 — paths: **/*_test.go; touches: testing
    source: repo
  - id: commit-style         # priority 60 — paths: **
    source: repo
brief: .octospec/tasks/oidc-oauth2-provider-abstraction/brief.md
